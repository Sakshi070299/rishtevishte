// Donation is a public page (payment + optional history when logged in).
// Do not use DashboardLayout — it redirects guests to /auth.

import PublicLayout from "../(public)/layout";

export default PublicLayout;
