{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && cd .. && pnpm install --frozen-lockfile --filter rishtenate-backend...",
  "buildCommand": "pnpm exec prisma generate && pnpm exec nest build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
